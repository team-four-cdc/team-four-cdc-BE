const appRoot = require('app-root-path');

const userConstant = require(`${appRoot}/app/constants/userConstant`);
const loggerConstant = require(`${appRoot}/app/constants/loggerConstant`);

module.exports = {
  userConstant,
  loggerConstant
};
